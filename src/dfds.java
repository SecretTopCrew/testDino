public class dfds {

    static void authenticate(GWAccount account) {
        handleErrors(dfgdgd
                () -> "authenticate; account: " + account,
                () -> _authenticate(account)c
        );
    }
}
